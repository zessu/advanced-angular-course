import { DlistenerDirective } from './dlistener.directive';

describe('DlistenerDirective', () => {
  it('should create an instance', () => {
    const directive = new DlistenerDirective();
    expect(directive).toBeTruthy();
  });
});
